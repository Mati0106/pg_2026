elements = ['iris', 'boston', 'titanic']
output_list = [el for el in elements if el =='Boston']

from pathlib import Path
cwd = Path.cwd()

# cond = zm > 4
# cond2 = zm >= 5

def load_data(input_list, cond):


# nowa_lista = [cond, cond2]

any([cond for cond in nowa_lista if cond == True])

path_datases = cwd.joinpath('datasets')

path_output_list = path_datases.joinpath(output_list[0])

# path.parents[2]
